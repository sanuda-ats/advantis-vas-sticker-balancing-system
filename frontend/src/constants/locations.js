// Hardcoded per current operations — move to a /api/locations endpoint
// later if the list needs to change without a frontend redeploy.
export const LOCATIONS = ["KDC VAS", "Setmil", "Lindel VAS", "Horana VAS"];